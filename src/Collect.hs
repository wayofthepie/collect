module Collect where

-- | Define what to collect using regular expressions.
-- collect  :: [Regex] -> Collection

-- | From the collection, use regex to define what to store and use
-- functions mapping collections to storage using regex.
-- store    :: [(Collection -> Regex -> Storage -> IO ())] -> IO ()

